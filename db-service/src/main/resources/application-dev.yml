spring:
  application:
    name: db-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/Retro}
    username: ${DB_USERNAME:retro_user}
    password: ${DB_PASSWORD:12345}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

logging:
  level:
    root: DEBUG
